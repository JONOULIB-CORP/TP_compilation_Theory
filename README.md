# TP_compilation_Theory
# Lancer le Commande 
pour executer un dossier le ouvrire le dossier i avec i etant de l'exercice 
ensuite dans le terminal tapez la commande :
``` cMake .
    cMake --build .
    ./exo{i} et l'argument a passe en parametre
